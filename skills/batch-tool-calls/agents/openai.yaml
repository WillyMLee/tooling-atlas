interface:
  display_name: "Batch Tool Calls"
  short_description: "Batch larger independent read sets safely"
  brand_color: "#35697B"
  default_prompt: "Use $batch-tool-calls to reduce round trips across this larger independent read set."
